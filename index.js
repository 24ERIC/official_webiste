// Solution to solve serverless function error when deploying vercel website

// Add Express
const express = require("express");

// Initialize Express
const app = express();

// Export the Express API
module.exports = app;